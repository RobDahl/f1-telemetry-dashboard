{
  "name": "f1-telemetry-dashboard",
  "version": "1.0.0",
  "description": "Monorepo for a FastAPI + Next.js F1 telemetry dashboard",
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,magenta \"python -m uvicorn main:app --reload --port 8000 --app-dir api\" \"npm run dev --prefix web\"",
    "dev:api": "python -m uvicorn main:app --reload --port 8000 --app-dir api",
    "dev:web": "npm run dev --prefix web"
  },
  "keywords": [
    "f1",
    "telemetry",
    "fastapi",
    "nextjs"
  ],
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
